{
  "name": "fdnix-search-lambda",
  "version": "0.1.0",
  "private": true,
  "description": "Rust-based AWS Lambda for fdnix hybrid search (custom runtime)",
  "scripts": {
    "build": "./build.sh",
    "test": "bash -lc 'command -v cargo >/dev/null && cargo test || echo \"cargo not found; skipping tests\"'"
  }
}

