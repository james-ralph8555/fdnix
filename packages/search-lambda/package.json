{
  "name": "fdnix-search-lambda",
  "version": "0.1.0",
  "private": true,
  "description": "C++ AWS Lambda for fdnix hybrid search with DuckDB (custom runtime)",
  "scripts": {
    "build": "./build.sh",
    "docker-build": "docker build -t fdnix-search-lambda .",
    "test": "echo \"C++ tests not implemented yet\"",
    "clean": "rm -rf build dist"
  }
}

